language: bash

before_script:
  - sudo add-apt-repository -y ppa:pgavin/ghdl
  - sudo apt-get -qq update
  - sudo apt-get install -y ghdl

script:
    - cd tps-Gaston/TP1-Contador
    - bash ./run.sh
